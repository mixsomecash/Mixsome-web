export type MoralisPairAddress = {
  pairAddress: string
  token0: {
    address: string
    name: string
    symbol: string
    decimals: string
  }
  token1: {
    address: string
    name: string
    symbol: string
    decimals: string
  }
}
