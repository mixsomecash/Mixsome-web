import React from 'react'

import { DashboardNetwork } from '../types'

type Props = { networkId: DashboardNetwork }

const NTFs = ({ networkId }: Props) => {
  return <div className="text-center my-20">NTFs coming soon...</div>
}

export default NTFs
