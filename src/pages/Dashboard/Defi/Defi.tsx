import React from 'react'

import { DashboardNetwork } from '../types'

type Props = { networkId: DashboardNetwork }

const Defi = ({ networkId }: Props) => {
  return <div className="text-center my-20">Defi coming soon...</div>
}

export default Defi
